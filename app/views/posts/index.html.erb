<div class="main posts-index">
  <div class="container">
    <% @posts.each do |post|%>
      <li>
        <div class="posts-index-item">
          <%= link_to("#{post.user.name}さんの投稿----:", "/users/#{post.user.id}")%>
          <%= link_to(post.content, "/posts/#{post.id}")%>
        </div>
      </li>
          <% end %>
  </div>
</div>